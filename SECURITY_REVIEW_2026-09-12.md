# مراجعة أمنية شاملة — EduQuest
**التاريخ:** 2026-09-12 · **النطاق:** كامل المشروع (252 ملف TypeScript، 47 مسار API، قاعدة البيانات الحية)

> ملف داخلي. لا ترفعه على الإنترنت ولا تضفه إلى Git — يحتوي على تفاصيل ثغرة قابلة للاستغلال.

---

## 1. الخلاصة

ثغرة **حرجة واحدة** وُجدت وأُثبت استغلالها فعلياً على قاعدة البيانات الحية، ثم أُغلقت وأُعيد اختبارها.
بقية النظام سليم إلى حد كبير: عزل المستأجرين (tenants) محكم، ومنطق الاختبارات موثوق من جهة الخادم،
ولا تسريب لأي مفتاح سري إلى المتصفح.

| الخطورة | العدد | الحالة |
|---|---|---|
| حرجة | 1 | ✅ مُصلحة ومُتحقق منها |
| متوسطة | 3 | ✅ مُصلحة |
| منخفضة / تحصين | 6 | ✅ مُصلحة |
| مقبولة بمبرر | 3 | موثّقة أدناه |
| تحتاج إجراء منك | 2 | انظر القسم 6 |

---

## 2. الثغرة الحرجة: تزوير الدرجات عبر الإدراج المباشر

**هذه أخطر نتيجة في المراجعة، وكانت مفتوحة على الإنتاج.**

### ما كان يحدث

هجرة `rpc_execute_lockdown_migration.sql` أغلقت مسار الـ RPC بشكل صحيح: صار
`finalize_exam_submission` يحسب الدرجة داخل قاعدة البيانات ويتجاهل أي درجة يرسلها العميل، وسُحبت
صلاحية EXECUTE من `anon` و`authenticated`. هذا العمل كان سليماً — لكنه أمّن **الدالة** فقط.

**الجدول نفسه بقي مفتوحاً.** جدول `exam_submissions` كان يحمل سياسة إدراج شرطها الوحيد:

```sql
WITH CHECK (student_id = auth.uid() AND tenant_id = current_tenant_id())
```

وكان الدور `authenticated` يملك صلاحية INSERT عادية. هذا الشرط يقيّد **لمن** تعود الصفيحة، ولا يقول
شيئاً عن `score` أو `max_score` أو `is_graded` أو `grading_status`، ولا يتحقق أن الطالب بدأ الاختبار
أصلاً أو أنه مسجّل في مجموعته.

### الاستغلال

أي طالب، بالمفتاح العام (anon key) وجلسته العادية فقط، كان يستطيع إرسال:

```
POST /rest/v1/exam_submissions
{ "exam_id": "...", "student_id": "<نفسه>", "tenant_id": "<مؤسسته>",
  "score": 100, "max_score": 100, "is_graded": true, "grading_status": "published" }
```

ويصنع لنفسه **درجة كاملة منشورة في اختبار لم يدخله أصلاً** — بل حتى في اختبار غير منشور، وفي مجموعة
غير مسجّل فيها. تم تنفيذ هذا فعلياً على قاعدة البيانات الحية داخل معاملة وأُرجعت (ROLLBACK)، وعادت
الصفيحة بـ `score: 100, grading_status: "published"`.

### الإصلاح

`supabase/fix_submission_insert_grade_forgery_migration.sql` — **طُبّق على القاعدة الحية**.

كل عمليات الكتابة المشروعة في المشروع تمر أصلاً عبر عميل `service_role`
(`api/exam/start`، `api/exam/submit`، `api/homework/submissions`)، ولا يوجد أي إدراج من المتصفح في
`src/` — لذلك حذف السياسة وسحب الصلاحيات لم يكسر شيئاً:

- حُذفت سياسة الإدراج وسُحبت صلاحيات INSERT/UPDATE/DELETE من `exam_submissions`
- نفس المعالجة لـ `unit_quiz_submissions` (نفس الشكل، ونفس عمود `score` القابل للكتابة، وغير مستخدم في الكود بعد)
- نفس المعالجة لـ `grades` (لا يكتبه أي كود، و`university_admin` مفترض أنه metadata-only)
- `student_progress` **تُرك عمداً** قابلاً للكتابة — مشغّل الكورسات يكتبه من المتصفح ولا يحتوي عمود درجات

**التحقق بعد الإصلاح:** أُعيد تنفيذ نفس الاستغلال فعُطّل، والجداول الثلاثة صارت SELECT فقط.

### الدرس المعمّم

الكتابة تحتاج **سياسة RLS وصلاحية GRANT معاً**، وهما بوّابتان مستقلتان. تحصين الدالة التي *يُفترض*
أنها الكاتب الوحيد لا يثبت شيئاً ما دام الجدول تحته قابلاً للكتابة مباشرة عبر PostgREST. وكل عمود
أمني (درجة، دور، صلاحية) يجب أن يُسأل عنه: هل يستطيع العميل ضبطه عند الإدراج؟ لأن `WITH CHECK` لا
تستطيع تقييد **أي الأعمدة** تُكتب، بل شكل الصفيحة النهائية فقط.

---

## 3. الثغرات المتوسطة (مُصلحة)

### 3.1 الواجبات: `group_id` بلا تحقق
`src/app/api/homework/route.ts` كان يتحقق من ملكية المعلم للدرس، لكنه يأخذ `group_id` من العميل
ويكتبه كما هو. معلم يستطيع نشر واجب في مجموعة زميله، أو في مجموعة تابعة لمؤسسة أخرى تماماً — لأن
`tenant_id` يُختم بمؤسسة المُرسِل فيبدو كل شيء سليماً. `api/exams` و`api/lessons` كلاهما يتحقق من
المجموعة؛ الواجبات كانت الاستثناء الوحيد. **أُضيف نفس التحقق.**

### 3.2 SSRF في معاينة الروابط
`src/app/api/announcements/preview-url/route.ts` يجعل الخادم يجلب أي رابط. التعليق في الملف كان يقول
إن اشتراط تسجيل الدخول يمنع إساءة الاستخدام — وهذا غير صحيح: أي حساب طالب هو جلسة صالحة. كان بالإمكان
توجيه الخادم إلى `169.254.169.254` (بيانات اعتماد السحابة) أو أي عنوان داخلي، ورسالة الخطأ كانت تُعيد
نص الخطأ الأصلي فتتحول إلى أداة استكشاف للشبكة الداخلية. **الإصلاح:** اشتراط صلاحية
`manage_announcements`، وحد معدّل، ومنع كل النطاقات الخاصة/المحلية/link-local، ومنع إعادة التوجيه
(`redirect: 'manual'`) حتى لا يقفز نطاق عام إلى عنوان داخلي، ورسالة خطأ ثابتة.

### 3.3 تعطيل الحساب لا يُطبَّق فوراً على الـ API
`is_active` يُقرأ من الـ JWT، والذي لا يُجدَّد إلا عند تحديث التوكن (~ساعة). ولا يعيد أي معالج API
قراءة الحقل. فحساب معلم عُطّل قبل دقيقة يبقى قادراً على الإنشاء والتصحيح حتى ساعة كاملة.
`<TenantWatcher>` يلتقط هذا خلال ~60 ثانية، لكنه يعمل في متصفح مفتوح فقط — سكربت يحمل التوكن يتجاهله.
**الإصلاح في `src/proxy.ts`:** إعادة قراءة `is_active` من قاعدة البيانات لطلبات **الكتابة فقط**
(POST/PATCH/PUT/DELETE على `/api/`)، فتبقى القراءات على المسار السريع المعتمد على الـ JWT ولا تُفقد
مكاسب الأداء التي جاءت من `getClaims()`.

---

## 4. إخفاء الكود المصدري وحماية البيانات الحساسة

### الخبر الجيد أولاً
**لا يمكن إخفاء كود الواجهة عن المتصفح — هذه حقيقة تقنية وليست خياراً.** أي شيء يعمل في المتصفح
يستطيع المستخدم قراءته. الشيء الوحيد الذي يهم فعلاً هو: ألا يوجد *خرائط مصدر* (source maps) تعيد
بناء كود TypeScript الأصلي، وألا يتسرب أي كود خادم أو مفتاح سري. **وهذا كان مضبوطاً أصلاً، وتحققت منه
بالبناء الفعلي لا بالافتراض:**

| ما فُحص | النتيجة |
|---|---|
| ملفات `.map` في `.next/static` (ما يخدمه المتصفح) | **صفر** |
| إشارات `sourceMappingURL` في حزم العميل | **صفر** |
| ملفات `.map` الموجودة (576) | كلها في `.next/server` و`.next/build` — لا تُخدم للمتصفح إطلاقاً |
| كود العميل | مضغوط ومشوّش بالكامل |
| متغيرات بيئة خادم في حزم العميل | **صفر** (فقط `NEXT_PUBLIC_*` وهي عامة بطبيعتها) |

### ما أضفته للتحصين
- `productionBrowserSourceMaps: false` صراحةً — حتى يصبح أي تفعيل لاحق قراراً مرئياً لا تغييراً صامتاً
- `sourcemaps.deleteSourcemapsAfterUpload: true` في إعداد Sentry — Sentry يولّد خرائط مصدر لفك رموز
  الأخطاء، وبدون هذا الإعداد قد ينتهي بها المطاف في المجلد المخدوم
- `poweredByHeader: false` — إزالة ترويسة `X-Powered-By: Next.js`
- **Content-Security-Policy** كامل (لم يكن موجوداً): الأهم فيه `connect-src` الذي يمنع أي سكربت
  مُحقَن من إرسال بيانات مسروقة إلى خادم خارجي، و`frame-ancestors 'none'` و`object-src 'none'`
- `Strict-Transport-Security` و`Cross-Origin-Opener-Policy` و`X-DNS-Prefetch-Control`
- `Cache-Control: no-store` على كل مسارات الأدوار — حتى لا تبقى صفحة اختبار في ذاكرة التخزين

**تُحقق فعلياً:** شُغّل خادم الإنتاج وفُحصت الترويسات فعادت كلها صحيحة، ثم فُتحت الصفحة الرئيسية
وصفحة الدخول وصفحة العرض التجريبي في متصفح حقيقي — **صفر أخطاء في الـ console**، أي أن الـ CSP
لم يكسر شيئاً.

### تسريب الملفات عند النشر
كان المجلد `public/capture-admin/` موجوداً — 28 ملفاً تشمل ملفات تعليمات داخلية (`AGENTS.md`،
`CLAUDE.md`، `.cursorrules`) ولقطات شاشة للوحة الإدارة. أي شيء داخل `public/` يُخدم على جذر الموقع،
فكان سيصبح متاحاً على `/capture-admin/...`. محتواه بيانات وهمية لا حقيقية، لكن لا مبرر لنشره.
**حُذف المجلد.**

وأضفت **`.vercelignore`** — وهذا مهم: `vercel --prod` يرفع *مجلد العمل* لا `git HEAD`، فكون الملف
غير متتبَّع في Git لا يمنع رفعه. الملف الجديد يستثني صراحةً: ملفات البيئة، السكربتات المؤقتة
(`_tmp_*` التي تقرأ `SUPABASE_SERVICE_ROLE_KEY`)، مجلد `supabase/` كاملاً (ينشر مخطط قاعدة البيانات
وسياسات RLS)، وكل ملفات التدقيق الداخلية.

**فحص الأسرار:** لا يوجد أي مفتاح حقيقي في أي ملف متتبَّع في Git. `.env.local` مستثنى بشكل صحيح.

---

## 5. ما فُحص ووُجد سليماً

- **عزل المستأجرين:** RLS مفعّل على كل الجداول الـ30. فُحصت سياسات الجداول الحساسة يدوياً.
- **دوال SECURITY DEFINER:** الـ12 دالة التي ينبّه عليها فاحص Supabase فُحصت كلها. الثلاث التي لا
  تأخذ معاملات (`get_student_exams/schedule/announcements`) تُقيّد بـ `auth.uid()` فلا يستطيع
  المستدعي رؤية غير بياناته. والاثنتان اللتان تأخذان معاملات (`get_course_progress`،
  `get_tenant_archive`) تتحققان من الدور **والمؤسسة** معاً — قرأت جسم `get_tenant_archive` بنفسي
  وأكدت شرط تطابق المؤسسة. **لا يوجد IDOR عبر المؤسسات.**
- **نزاهة الاختبارات:** التوقيت والتصحيح من جهة الخادم، `correct_answer` لا يغادر الخادم،
  `finalize_exam_submission` يعيد الحساب داخل القاعدة ويتجاهل درجة العميل. سليم.
- **مسارات الـ API الـ47:** الثلاثة العامة فقط بلا مصادقة، وكلها مقصودة ومحدودة بمعدّل
  (`accept-invitation`، `forgot-password`، `contact`). البقية تُصادق ثم تُصرّح.
- **تصعيد الصلاحيات:** نموذج القدرات في `src/lib/permissions.ts` سليم — لا أحد يمنح صلاحية لا يملكها،
  والدور يُفحص قبل قراءة خريطة الصلاحيات، و`users_update` يثبّت أعمدة التصريح فلا يمكن الرفع الذاتي.
- **الدعوات:** أدوار الموظفين لا تُمنح عبر رابط عام أبداً، وسقف الأدوار محترم، والقبول ذرّي تحت قفل.
- **العرض التجريبي `/demo`:** بيانات ثابتة بالكامل، **صفر** اتصال بقاعدة البيانات. لا خطر منه.
- **TypeScript:** `tsc --noEmit` يمر **نظيفاً تماماً** (0 أخطاء) — لا أخطاء أنواع ولا `any` متسيّب.

---

## 6. يحتاج إجراءً منك

1. **فعّل حماية كلمات المرور المسرَّبة** — معطّلة حالياً. لوحة Supabase ← Authentication ←
   Password Protection. تمنع استخدام كلمات مرور ظهرت في تسريبات سابقة. إعداد لا يُضبط بـ SQL.
2. **`_tmp_admin.mjs` ما زال في مجلد العمل** — إعادة تشغيله تُنشئ حساب مدير بكلمة مرور نصية صريحة،
   وهذا سيكسر حالة "المالك فقط" التي وصلنا إليها للتو. احذفه إن لم تعد تحتاجه.

### مقبول بمبرر (لا تغيير)
- `rate_limits` بلا سياسات RLS — مقصود، يُكتب بـ service_role فقط.
- دوال `current_*()` قابلة للاستدعاء من `anon` — تُرجع NULL لغير المسجّل لأنها تعتمد `auth.uid()`.
  **تعمّدت عدم سحب الصلاحية**: سياسات RLS تستدعي هذه الدوال، وسحب EXECUTE قد يكسر تقييم السياسات
  نفسها. المكسب معدوم والمخاطرة حقيقية.

---

## 7. حالة قاعدة البيانات بعد التفريغ

نُفّذ `supabase/clean_all_test_data_keep_owner.sql` وتم التحقق:

| الجدول | قبل | بعد |
|---|---|---|
| `auth.users` | 4 | **1** (المالك) |
| `public.users` | 4 | **1** |
| `tenants` / `groups` / `exams` / `invitations` | 1 لكل منها | **0** |
| `staff_requests` / `request_messages` | 2 / 2 | **0** |
| `rate_limits` | 68 | **0** |
| `platform_settings` | 4 | **4** (إعدادات، أُبقيت) |
| بقية الجداول الـ22 | 0 | **0** |

الحساب الباقي: `sbig61867@gmail.com` — super_admin، مفعّل، مؤكَّد البريد، ومطالبات الـ JWT سليمة
(`user_role: super_admin`). السكربت محمي بحارسين: يرفض العمل إن لم يكن هناك super_admin واحد بالضبط،
ويتحقق من نجاة المالك بعد الحذف.

**ملاحظة:** الحذف تم من `auth.users` لا من `public.users` — لأن الصفيحة العامة تتتالى من هوية
المصادقة، فحذف الملف الشخصي وحده كان سيترك هوية حية قادرة على تسجيل الدخول.

---

## 8. أمور قائمة لم أغيّرها

- **12 اختبار فاشل** في `src/__tests__/mutations-extended.test.tsx` (اختبارات واجهة
  `CourseBuildClient`). **أثبتّ أنها فاشلة قبل تغييراتي** بإرجاع التعديلات وإعادة التشغيل — نفس
  الـ12 تفشل. ليست من عملي، لكنها تحتاج إصلاحاً. الباقي 51 اختبار يمر.
- **خطأ ESLint واحد** في `src/components/student/quick-access-panel.tsx:77` (استدعاء setState داخل
  useEffect). موجود في كود مُودَع ولم ألمسه.
- **`center_manager` لا يستطيع إنشاء دعوات** رغم وجود صلاحية `manage_invitations` في نموذج القدرات —
  `ROLE_CEILING` في مسار الدعوات لا يتضمنه، ولا توجد صفحة دعوات في `(center)`. تعارض وظيفي لا أمني.

---

## الملفات المتغيّرة

```
جديد:      supabase/fix_submission_insert_grade_forgery_migration.sql   (طُبّق على القاعدة الحية)
جديد:      supabase/clean_all_test_data_keep_owner.sql                   (نُفّذ)
جديد:      .vercelignore
معدّل:     next.config.ts                                    (CSP + HSTS + منع خرائط المصدر)
معدّل:     src/proxy.ts                                      (فحص is_active على الكتابات)
معدّل:     src/app/api/homework/route.ts                     (تحقق group_id)
معدّل:     src/app/api/announcements/preview-url/route.ts    (إغلاق SSRF)
معدّل:     src/app/api/exam/submit/route.ts                  (حجب الدرجة غير المنشورة)
معدّل:     supabase/schema.sql · CLAUDE.md                   (توثيق الحالة الجديدة)
محذوف:     public/capture-admin/                             (28 ملف)
```

لم أنفّذ أي commit ولا push ولا نشر — حسب تفضيلك المسجّل.
