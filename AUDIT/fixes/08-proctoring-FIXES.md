# تقرير إصلاحات — المرحلة 8: المراقبة بالكامل على جهاز الطالب

**التاريخ:** 2026-09-13 · **الحالة:** مُطبَّق محلياً، لم يُرفع لـ git · **لا تغيير على قاعدة البيانات**
**تقرير الاكتشافات المرتبط:** [AUDIT/08-ai-cost-model.md](../08-ai-cost-model.md)

## ⚠️ تصحيح أولاً: اكتشاف المرحلة 8 الأصلي كان خاطئاً في جوهره

كتبتُ أن المراقبة "ترسل صورة لـ Gemini كل 30 ثانية لكل طالب" وأن "السقف الآمن ~7 طلاب". **هذا لم يكن يحدث فعلياً.** قرأتُ مسار الخادم `/api/proctor/analyze` ولم أتحقق هل يستدعيه أي كود في المتصفح. الواقع بعد قراءة [exam-taker.tsx](../../src/app/(student)/student/exams/exam-taker.tsx) والـ hooks:

- طبقة Gemini (`use-server-proctoring.ts`) كانت **معطّلة افتراضياً** خلف متغير `NEXT_PUBLIC_SERVER_PROCTORING` (قيمته `false` في `.env.local`).
- **الكشف كان يعمل أصلاً على جهاز الطالب:** MediaPipe (الوجه والنظر) كل 4 ثوانٍ، TensorFlow.js COCO-SSD (هاتف/كتاب/شخص إضافي) كل 5 ثوانٍ، والصوت وتبديل التبويب والخروج من ملء الشاشة.
- **التجميع موجود أصلاً:** `use-proctor-recorder.ts` يدمج التكرارات ويرسل دفعة كل 15 ثانية إلى `/api/proctor/events` بلا أي ذكاء اصطناعي.

أي أن ما طلبتَه كان **مُنفَّذاً بنسبة كبيرة**. الإصلاحات أدناه أكملت الباقي وأغلقت نقاط ضعف حقيقية وجدتها أثناء القراءة.

## ما أُصلح

| # | المشكلة الحقيقية | الإصلاح | الملفات |
|---|---|---|---|
| 1 | **مسار Gemini ما زال موجوداً ويمكن استدعاؤه مباشرة** رغم تعطيله في الواجهة: أي طالب مسجَّل كان يستطيع إرسال صور إلى `/api/proctor/analyze` واستهلاك حصة Gemini | **حذفت الطبقة بالكامل** حسب قرارك "المراقبة كاملاً على جهاز المستخدم": المسار، الـ hook، المتغير، والـ canvas المخفي. الحذف قابل للاسترجاع من git (الملفان كانا مرفوعين وغير معدَّلين) | حُذف: `src/app/api/proctor/analyze/route.ts`، `src/hooks/use-server-proctoring.ts` · عُدِّل: [exam-taker.tsx](../../src/app/(student)/student/exams/exam-taker.tsx) |
| 2 | **MediaPipe يُحمَّل بإصدار `@latest`** من الـ CDN — أي إصدار مستقبلي يغيّر ملفات WASM كان سيعطّل كشف الوجه **لكل الاختبارات بصمت** | ثبّتُ الإصدار على `0.10.35` (المُثبَّت في `package.json`) | [use-face-detection.ts](../../src/hooks/use-face-detection.ts) |
| 3 | الكشف يطلب GPU فقط؛ على جهاز بلا WebGL يفشل التحميل ولا يعمل كشف الوجه إطلاقاً | **محاولة GPU أولاً ثم CPU تلقائياً** | [use-face-detection.ts](../../src/hooks/use-face-detection.ts) |
| 4 | على جهاز بطيء، تحليل TensorFlow قد يستغرق أكثر من 5 ثوانٍ، و`setInterval` كان **يكدّس تحليلات متداخلة** ويستنزف معالج/بطارية الطالب طوال الاختبار | حارس "مشغول": تُتخطّى الدورة إن كانت السابقة لم تنتهِ | [use-object-detection.ts](../../src/hooks/use-object-detection.ts) |
| 5 | **فشل تحميل أي كاشف كان يُبتلع بصمت** (`init().catch(() => {})`) — المعلم يرى "Clean" لطالب لم يُراقَب فعلياً | نوع حدث جديد `detector_unavailable` يُسجَّل للمعلم. **لا يُحتسب مخالفة** ولا يظهر تنبيه للطالب (ليس ذنبه) | الـ hooks، [exam-taker.tsx](../../src/app/(student)/student/exams/exam-taker.tsx)، [proctor/events](../../src/app/api/proctor/events/route.ts)، [types](../../src/types/index.ts) |
| 6 | صفحة المعلم ونتائج الاختبار كانت ستعدّ هذا الحدث "مخالفة" | استُبعد من العدّ في [صفحة تقارير المراقبة](../../src/app/(teacher)/teacher/proctoring/page.tsx) و[exams/results](../../src/app/api/exams/results/route.ts)، مع **تنبيه أصفر منفصل** للمعلم: "المراقبة غير مكتملة على جهاز هذا الطالب — غياب المخالفات هنا ليس دليل نزاهة" | الملفان المذكوران |
| 7 | إعدادات super-admin ما زالت تعرض "Server-side proctoring analysis: Disabled" | تعرض الآن "On student device (no AI cost)" | [settings/page.tsx](../../src/app/(super-admin)/super-admin/settings/page.tsx)، [settings-client.tsx](../../src/app/(super-admin)/super-admin/settings/settings-client.tsx) |
| 8 | التوثيق يصف المراقبة بأنها عبر Gemini | حُدِّث `CLAUDE.md` و`.env.example` و`DEPLOYMENT.md`، وتعليقات `exam/submit` | الملفات المذكورة |

## ما لم أفعله عمداً
**لم أضف "استدعاء نموذج لغوي عند الاشتباه".** ذكرتَه ضمن البدائل، ثم حدّدتَ القرار: "المراقبة كاملاً من خلال موارد المستخدم". إضافة استدعاء خادم، ولو نادراً، تعيد الاعتماد على حصة خارجية. إن أردته لاحقاً كتأكيد اختياري، يُبنى بسقف صارم لكل اختبار.

## التحقق
- `tsc --noEmit` ✅ (بعد حذف ملف أنواع مولَّد قديم في `.next/types` كان يشير للمسار المحذوف — مجلد بناء مُتجاهَل في git)
- `npm run lint` ✅ · `vitest` **63/63** ✅
- لا مرجع متبقٍ للملفات المحذوفة في `src/`
- **في متصفح حقيقي:** حُمِّل MediaPipe بالإصدار المثبَّت `0.10.35` من الـ CDN مع النموذج، **وعمل على GPU الجهاز**، ونفّذ كشفاً محلياً على صورة (0 وجوه على إطار فارغ كما هو متوقع)
- روابط WASM والنموذج المثبَّتة ترجع HTTP 200

**حدود التحقق (بصراحة):** لم أختبر شاشة الاختبار كاملة بكاميرا حقيقية — متصفح المعاينة لا يملك كاميرا، ويتطلب ذلك حساب طالب واختباراً مراقَباً. أنصح بتجربة اختبار مراقَب واحد بنفسك على `localhost` قبل النشر، والتأكد من ظهور المخالفات عند تغطية الكاميرا أو إظهار هاتف.

## الأثر على التكلفة والتوسع
- **تكلفة الذكاء الاصطناعي للمراقبة: صفر**، مهما كان عدد الطلاب.
- الحمل على الخادم لكل طالب أثناء الاختبار: دفعة أحداث صغيرة كل 15 ثانية (فقط إن وُجدت أحداث) + حد أقصى 10 صور أدلة للمخالفات الشديدة.
- **العبء الحقيقي الآن على جهاز الطالب:** الأجهزة الضعيفة جداً قد تُبطئ أثناء الاختبار. الحارس في البند 4 والتراجع لـ CPU في البند 3 يخففان ذلك، والتسجيل في البند 5 يكشف أي جهاز فشل فيه الكشف.
